/**
 * Interface Category - Collections/Catégories de parfums
 *
 * Utilisé pour la grille de navigation style Louis Vuitton
 * (Pas de prix, focus sur l'univers)
 */
export interface Category {
  id: string;
  name: string;
  image: string;
  link: string;
}
